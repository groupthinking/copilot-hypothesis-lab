# Space Manifest — 2k (Minimal Context)

## Metadata
- **Space ID**: `h3-2k`
- **Target Context Tokens**: ~2,000
- **Actual Context**: Task + project scaffolding

## Purpose
Minimal viable context. Provides just enough project structure for the agent to understand the technology stack and file layout.

## Context Included

1. **Task description** (`task/oauth2_device_flow.md`) — ~400 tokens
2. **README.md** — project overview, ~300 tokens
3. **package.json** — dependencies and scripts, ~200 tokens
4. **Directory tree** — top-level structure listing, ~100 tokens
5. **app.js / index.ts** — entry point (first 50 lines only), ~400 tokens
6. **Example route file** — one existing route for style reference, ~600 tokens

**Total**: ~2,000 tokens

## What the Agent Knows

- Stack: Node.js + Express
- Project entry point location
- Route file conventions
- Installed dependencies (from package.json)
- One example of how routes are structured

## What the Agent Must Infer

- Middleware chain
- Error handling conventions
- Test framework and patterns
- Auth token storage approach
- Environment variable conventions

## Hypothesis Role

First non-zero data point. Tests whether minimal scaffolding context meaningfully improves over zero context.
