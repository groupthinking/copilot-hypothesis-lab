# Space Manifest — 32k (Rich Context)

## Metadata
- **Space ID**: `h3-32k`
- **Target Context Tokens**: ~32,000
- **Actual Context**: Full codebase (without test suite)

## Purpose
Rich context. Provides the entire application codebase so the agent has complete picture of all existing code patterns, utilities, and conventions.

## Context Included

1. **Task description** (`task/oauth2_device_flow.md`) — ~400 tokens
2. **Full application codebase** — all source files, ~20,000 tokens
   - Routes, controllers, services, models, middleware, utils
   - TypeScript types and interfaces
   - Config and environment handling
3. **All existing documentation** — inline JSDoc, ~3,000 tokens
4. **CONTRIBUTING.md and code style guide** — ~1,500 tokens
5. **Database schema / migrations** — ~2,000 tokens
6. **API documentation** — existing endpoints spec, ~2,500 tokens
7. **Security policies** — `SECURITY.md`, ~1,000 tokens
8. **OAuth2 RFC 8628 summary** — device flow specification excerpt, ~1,600 tokens

**Total**: ~32,000 tokens

## What the Agent Knows

Everything from 8k, plus:
- Complete codebase conventions and patterns
- All utility functions available
- Database schema for token storage
- Security requirements
- OAuth2 RFC 8628 core requirements

## What the Agent Must Infer

- Integration edge cases not covered in RFC summary
- Test strategy (test suite not included)

## Hypothesis Role

Tests the "full codebase without tests" context level. Expected to show strong improvement over 8k as the agent can reuse existing patterns precisely.
