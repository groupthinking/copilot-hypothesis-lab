# Space Manifest — 8k (Moderate Context)

## Metadata
- **Space ID**: `h3-8k`
- **Target Context Tokens**: ~8,000
- **Actual Context**: Core application files + auth patterns

## Purpose
Moderate context. Provides the full application core so the agent understands architecture, middleware, and existing auth patterns.

## Context Included

1. **Task description** (`task/oauth2_device_flow.md`) — ~400 tokens
2. **README.md** — full, ~500 tokens
3. **package.json** — ~200 tokens
4. **app.js / app.ts** — full application setup, ~800 tokens
5. **All existing route files** — ~1,500 tokens
6. **Middleware directory** — auth middleware, error handler, ~1,000 tokens
7. **Config files** — `.env.example`, database config, ~400 tokens
8. **Existing auth flow** — if any (JWT, session), ~1,200 tokens
9. **TypeScript types / interfaces** — ~800 tokens
10. **One existing test file** — for test style reference, ~1,200 tokens

**Total**: ~8,000 tokens

## What the Agent Knows

Everything from 2k, plus:
- Full middleware chain
- Error handling conventions
- Existing auth patterns and token storage
- Test framework and test style
- Environment variable names

## What the Agent Must Infer

- Complete OAuth2 device flow RFC compliance details
- Edge cases not demonstrated in existing code

## Hypothesis Role

Tests whether knowing the full application core (but not docs/RFCs) produces meaningful quality gains.
